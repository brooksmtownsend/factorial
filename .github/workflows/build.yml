name: Build and Test
on:
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "Cargo.*"
jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check formatting
        run: cargo fmt -- --check
        shell: bash
      - name: Build provider
        run: cargo build
      - name: Check lints with clippy
        run: |
          rustup component add clippy
          cargo clippy
      # Optional, if your integration tests depend on NATS or Redis
      # - uses: wasmcloud/common-actions/run-nats@main
      # - uses: wasmcloud/common-actions/run-redis@main
      - name: Test provider
        run: cargo test -- --nocapture
